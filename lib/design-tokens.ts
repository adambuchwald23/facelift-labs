/**
 * Shared design tokens — eliminates duplicated shadow constants
 * across section components. Import from here instead of re-declaring per file.
 */

export const CARD_SHADOW =
  "0 1px 2px rgba(0,0,0,0.04), 0 4px 12px rgba(0,0,0,0.05), 0 16px 40px rgba(0,0,0,0.06)";
